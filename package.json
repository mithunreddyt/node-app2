{
  "name": "nodeapp-2",
  "version": "1.0.0",
  "description": "This is a app installed with HTTP server by using EXPRESS(a NODE framework)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mithun",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "update": "^0.7.4"
  }
}
